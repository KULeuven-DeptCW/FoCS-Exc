\begin{tikzpicture}[shorten >=1pt,node distance=2cm,on grid,auto,scale=0.75]
\def\sc{0.5}
\foreach \a in {0,1,2,3} {
  \foreach \b in {0,1,2,3} {
    \foreach \c in {0,1,2,3} {
      \foreach \d in {0,1,2,3} {
	\node[state,scale=\sc] (q\a\b\c\d) at (4*\a+\c,4*\b+\d) {$q_{\a,\b}^{\c,\d}$};
      }
    }
  }
}
\foreach \ea in {0,1,2,3} {
  \foreach \eb in {0,1,2,3} {
    \foreach \ec in {0,1,2,3} {
      \foreach \od in {0/1,1/2,2/2,3} {
      }
    }
  }
}
\end{tikzpicture}